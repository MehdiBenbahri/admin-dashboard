<turbo-stream action="refresh" target="target_row_{{ rowId }}"></turbo-stream>
<turbo-stream action="append" target="flash" >
    <template>
        <div class="alert alert-{{ type }}">
            {{ message }}
        </div>
    </template>
</turbo-stream>